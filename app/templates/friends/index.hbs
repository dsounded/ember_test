<h1>Friends Index</h1>
<ul>
{{#if model.length}}
  Total amount of friends: {{model.length}}
{{else}}
  There is no friends at all.
{{/if}}
<hr>
  {{#each model as |friend|}}
    <li>
      {{link-to friend.fullName 'friends.show' friend}}
    </li>
  {{/each}}
</ul>